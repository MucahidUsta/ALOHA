import React from 'react';
import {Text} from 'react-native';

const MyTextView = ({count})=>{
    return(
        <>
        <Text>{count}</Text>
        </>
    );
  
}
export default MyTextView;